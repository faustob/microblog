# Instrumentation for Microblog

Generated by TelemetryOS against `a975ef6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (5):**
- qr-not-a-noop (`app/telemetry.py`): microblog.py imports app.telemetry for side effect, but the module only DEFINES init_telemetry() and never calls it at module scope; nothing in this diff invokes init_telemetry(), so no provider is ever registered and the FlaskInstrumentor is never wired — every meter/tracer stays a no-op.
- qr-not-a-noop (`app/vitals_routes.py`): The 'vitals' Blueprint defining /api/vitals is never registered with the Flask app (no create_app edit registers it), so the endpoint does not exist and the LCP/INP histograms never record.
- qr-metric-naming (`app/vitals_routes.py`): New histogram names 'web.vital.lcp' and 'web.vital.inp' use unit 'ms' but the name lacks the recorded-unit suffix expected by semconv style for durations, and Web Vitals durations are conventionally recorded in seconds; the name does not convey the measured unit.
- qr-not-a-noop (`microblog.py`): microblog.py imports app.telemetry for side effect, but app/telemetry.py performs NO module-level setup — its provider registration lives inside init_telemetry(), which is never called anywhere in this diff. As a result no SDK is ever registered and every tracer/meter/histogram remains a no-op.
- qr-not-a-noop (`app/vitals_routes.py`): The vitals blueprint (and thus its histograms and /api/vitals route) is only registered inside init_telemetry() when app is not None, but init_telemetry is never invoked; the blueprint is never registered, so the endpoint 404s and the web-vitals metrics never emit.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`microblog.py`:1): microblog.py imports `app.telemetry` for side effect, but app/telemetry.py only defines init_telemetry() and has NO module-level setup statements, so the import registers nothing. Registration actually happens via create_app()->init_telemetry(app), so the import comment is misleading but the SDK is still registered elsewhere.
- ⚠ qr-metric-naming (`app/vitals_routes.py`:10): The new histogram names 'web.vital.lcp.duration' and 'web.vital.inp.duration' encode the recorded unit as seconds via the '.duration' + unit='s' convention, but LCP/INP are recorded here divided by 1000 (ms->s) which is fine; however the names introduce a 'web.vital.*' domain prefix — acceptable as the single established style, but INP is an interaction latency and LCP a render time, so 'duration' is a reasonable measurement noun.

## SLIs implemented
- **Microblog — Largest Contentful Paint (p75)** (`microblog-web-lcp-p75`) — target < 2.5s over 28-day
- **Microblog — Interaction to Next Paint (p75)** (`microblog-web-inp-p75`) — target < 200ms over 28-day

## Modified files
- `app/auth/routes.py`
- `microblog.py`
- `app/__init__.py`

## New files
- `app/telemetry.py`
- `app/vitals_routes.py`
- `app/static/js/vitals.js`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp-proto-http`
- `opentelemetry-instrumentation-flask`
- `opentelemetry-exporter-otlp`
