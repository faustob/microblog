# Instrumentation for Microblog

Generated by TelemetryOS against `a975ef6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-unwired-telemetry (`app/vitals.py`): vitals_js() serves the reporter at endpoint name 'auth.vitals_js' but the JS's POST target is resolved via url_for('auth.web_vitals'), while the route registered is named 'web_vitals' on the auth blueprint — the endpoint reference used inside VITALS_JS is url_for('auth.web_vitals') which is correct, but inject_vitals_snippet references url_for('auth.vitals_js'). These are consistent, so this is not the defect.
- qr-export-pipeline (`app/telemetry.py`): The MeterProvider and TracerProvider are built but never shut down (no atexit/shutdown hook), so buffered histogram measurements in the PeriodicExportingMetricReader and spans in the BatchSpanProcessor are lost when the process exits — the RUM measurements may never leave the process reliably.
**Outstanding (1):**
- ⚠ qr-metric-naming (`app/telemetry.py`:80): New metric names 'web.vital.lcp' and 'web.vital.inp' are recorded in milliseconds with unit='ms' but carry no unit suffix, while the SLI convention and semconv prefer duration names to reflect their recorded unit; the names also omit any duration suffix (e.g. web.vital.lcp.duration). Consider naming them to state the measured duration and unit consistently.

## SLIs implemented
- **Microblog — Largest Contentful Paint (p75)** (`microblog-web-lcp-p75`) — target < 2.5s over 28-day
- **Microblog — Interaction to Next Paint (p75)** (`microblog-web-inp-p75`) — target < 200ms over 28-day

## Modified files
- `app/auth/routes.py`
- `microblog.py`

## New files
- `app/telemetry.py`
- `app/vitals.py`

## Dependencies
- `opentelemetry-api~=1.44.0 (add to requirements.txt)`
- `opentelemetry-sdk~=1.44.0 (add to requirements.txt)`
- `opentelemetry-exporter-otlp~=1.44.0 (add to requirements.txt)`
- `browser-side web-vitals@4 is loaded from the CDN by app/vitals.py — no build dependency needed`
