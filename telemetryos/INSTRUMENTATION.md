# Instrumentation for Microblog

Generated by TelemetryOS against `a975ef6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (2):**
- ⚠ qr-metric-naming (`app/telemetry.py`:24): The two new duration histograms record in different units — `web.vital.lcp` is recorded in seconds (unit='s') while `web.vital.inp` is recorded in milliseconds (unit='ms') — and neither name carries its recorded unit, so the same concept (a web-vital latency) is spelled two different ways and a dashboard cannot tell which scale a series uses. — _Pick one unit convention for both vitals (e.g. seconds, per semconv duration style) and encode it in the names, e.g. `web.vital.lcp.duration`/`web.vital.inp.duration` with unit='s' (converting INP ms→s in the /vitals route), keeping WEB_VITAL_UNITS consistent._
- ⚠ general (`app/main/routes.py`:170): The unauthenticated /vitals endpoint copies client-supplied `route`, `rating` and `device_class` values straight into metric attributes (only length-truncated), so any caller can inject arbitrary attribute values and cause unbounded time-series cardinality in the LCP/INP histograms. — _Validate the incoming values against allow-lists (e.g. only routes that match a registered Flask url_rule, rating in {good,needs-improvement,poor}, device_class in {mobile,desktop}) and fall back to 'other'/'unknown' for anything else before recording._

## SLIs implemented
- **Microblog — Largest Contentful Paint (p75)** (`microblog-web-lcp-p75`) — target < 2.5s over 28-day
- **Microblog — Interaction to Next Paint (p75)** (`microblog-web-inp-p75`) — target < 200ms over 28-day

## Modified files
- `app/__init__.py`
- `app/main/routes.py`
- `app/templates/base.html`
- `requirements.txt`
- `microblog.py`

## New files
- `app/telemetry.py`

## Dependencies
- `opentelemetry-api==1.44.0`
- `opentelemetry-sdk==1.44.0`
- `opentelemetry-exporter-otlp==1.44.0`
- `opentelemetry-instrumentation-flask==0.65b0`
