# Instrumentation for Microblog

Generated by TelemetryOS against `a975ef6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-not-a-noop (`microblog.py`): `import app.telemetry` only imports the module; the module has no top-level `init_telemetry()` call, so importing it registers NOTHING. The comment claims module-level activation but the SDK setup lives entirely inside `init_telemetry()`, which this import never invokes.
- qr-metric-naming (`app/telemetry.py`): The histogram `web.vital.lcp` records seconds (value/1000.0) but its unit is 's' while `web.vital.inp` records raw milliseconds — the same conceptual metric family (web vitals) uses inconsistent units and none carry a unit suffix in the name, and `web.vital.value` for 'other' vitals mixes CLS (unitless) with ms.
**Outstanding (1):**
- ⚠ qr-not-a-noop (`microblog.py`:1): microblog.py imports `app.telemetry` for side effect claiming module-level setup activates the SDK, but telemetry.py performs registration only inside init_telemetry() with no module-level call; the import alone registers nothing. Registration actually happens via create_app()->init_telemetry(app), so the misleading import comment is harmless but the stated activation mechanism does not exist.

## SLIs implemented
- **Microblog — Largest Contentful Paint (p75)** (`microblog-web-lcp-p75`) — target < 2.5s over 28-day
- **Microblog — Interaction to Next Paint (p75)** (`microblog-web-inp-p75`) — target < 200ms over 28-day

## Modified files
- `app/__init__.py`
- `app/main/routes.py`
- `app/templates/base.html`
- `requirements.txt`
- `microblog.py`

## New files
- `app/telemetry.py`

## Dependencies
- `opentelemetry-api==1.44.0`
- `opentelemetry-sdk==1.44.0`
- `opentelemetry-exporter-otlp==1.44.0`
