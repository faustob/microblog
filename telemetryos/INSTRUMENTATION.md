# Instrumentation for Microblog

Generated by TelemetryOS against `a975ef6`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (5):**
- qr-not-a-noop (`app/telemetry.py`): init_telemetry() is defined but never called anywhere in this diff, and no app factory/entrypoint invokes it. Under app-sdk with no existing registration files, nothing registers the SDK, so the histograms resolve to the no-op ProxyMeterProvider and emit nothing.
- general (`app/api/routes.py`): The sendBeacon path sends a Blob with Content-Type 'application/json', but the server relies on Flask's get_json which requires the JSON mimetype; more importantly the response returns status 204 with a JSON body, and beacon 204 responses discard the body — behavior is fine but the 204+jsonify body is inconsistent.
- qr-not-a-noop (`app/__init__.py`): create_app imports init_telemetry and FlaskInstrumentor but never calls init_telemetry() or setup_otel(app), so the global SDK providers are never registered and the Flask app is never instrumented. Under app-sdk wiring this diff must register the SDK; the meters/tracers resolve to the no-op proxy and emit nothing.
- qr-not-a-noop (`app/telemetry.py`): init_telemetry wraps set_tracer_provider/set_meter_provider in a broad try/except that swallows all exceptions; if provider construction fails the app silently emits no telemetry, and the comment's premise is wrong since set_*_provider never raises on re-registration.
- general (`app/telemetry.py`): The metric names 'web.vital.lcp' and 'web.vital.inp' use a millisecond value but the unit is 'ms' with no suffix reflecting the recorded unit; per semconv style durations should carry their unit. Consider aligning the name/unit convention.
**Outstanding (1):**
- ⛔ qr-not-a-noop (`app/telemetry.py`): init_telemetry() builds and registers the SDK providers but is never called anywhere in this diff (no app factory, WSGI/ASGI entrypoint, or startup hook invokes it), and FlaskInstrumentor().instrument_app(app) is never wired. Under app-sdk with no agent, nothing registers the SDK, so the histograms resolve to the no-op proxy and emit nothing. — _Call init_telemetry() (and FlaskInstrumentor().instrument_app(app)) in the Flask app factory / create_app at startup before serving._

## SLIs implemented
- **Microblog — Largest Contentful Paint (p75)** (`microblog-web-lcp-p75`) — target < 2.5s over 28-day
- **Microblog — Interaction to Next Paint (p75)** (`microblog-web-inp-p75`) — target < 200ms over 28-day

## Modified files
- (none)

## New files
- `app/telemetry.py`
- `app/api/routes.py`
- `app/static/js/web-vitals-report.js`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp-proto-http`
- `opentelemetry-instrumentation-flask`
